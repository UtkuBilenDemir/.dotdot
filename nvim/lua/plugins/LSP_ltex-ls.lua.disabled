require("lspconfig").ltex.setup({
  settings = {
    ltex = {
      language = "en-GB",
      checkFrequency = "save",

      -- enable grammar + style rules
      additionalRules = {
        enablePickyRules = true,
      },

      -- optional: disable only what you really hate
      disabledRules = {
        ["en-GB"] = {
          "PASSIVE_VOICE", -- optional
        },
      },
    },
  },
})
